import { View } from "react-native-reanimated/lib/typescript/Animated";
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
  } from 'react-native';

function Home(){
    return(
        <SafeAreaView>
            <Text>Sup, jonge!</Text>
        </SafeAreaView>
    )
}

export default Home;