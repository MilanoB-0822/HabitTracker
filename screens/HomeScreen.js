import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View
  } from 'react-native';

function HomeScreen(){
    return(
        <SafeAreaView>
            <Text>Sup, jonge!</Text>
        </SafeAreaView>
    )
}

export default HomeScreen;